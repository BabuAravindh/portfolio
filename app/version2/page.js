import TerminalPageClient from './TerminalPageClient';

export const metadata = {
  title: 'Babu Aravindh | CLI Terminal Portfolio',
  description: 'Interactive command-line interface portfolio of Babu Aravindh. Type commands to explore projects, skills, and contact info.',
};

export default function TerminalPage() {
  return <TerminalPageClient />;
}
